# Frontend Environment Variables Example
# Copy this file to .env.local for local development

# API Base URL (for production deployment)
# Leave empty for local development (will use Next.js proxy)
# For production (e.g., Render), set to your backend API URL
# Example: https://your-backend-api.onrender.com/api
NEXT_PUBLIC_API_BASE_URL=

# Note: All NEXT_PUBLIC_* variables are exposed to the browser
# Do NOT put sensitive information (like API keys or secrets) here
