# Backend Environment Variables for Production Deployment
# Set these in your Render dashboard or deployment platform

# ==========================================
# MongoDB Configuration (REQUIRED)
# ==========================================
# Your MongoDB Atlas connection string
# Get this from MongoDB Atlas dashboard -> Connect -> Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# MongoDB database name (default: portfolio)
MONGODB_DATABASE=portfolio

# ==========================================
# Authentication Configuration (REQUIRED)
# ==========================================
# Admin username for dashboard login
ADMIN_USERNAME=your-admin-username

# Admin password for dashboard login (use a strong password)
ADMIN_PASSWORD=your-secure-password

# ==========================================
# CORS Configuration (REQUIRED for Production)
# ==========================================
# Comma-separated list of allowed origins
# Example: https://your-frontend.onrender.com,http://localhost:3000
# For multiple origins, separate with commas (no spaces)
ALLOWED_ORIGINS=https://your-frontend-url.com

# ==========================================
# Server Configuration (Optional)
# ==========================================
# Port number (Render sets this automatically)
PORT=8080

# Logging level (INFO, DEBUG, WARN, ERROR)
LOGGING_LEVEL=INFO
